<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcm.community.mapper.NotifyPbravoMapper">
    <sql id="allCol">
        npbid,nid,fuid,tuid,pid,title,isdel,time
    </sql>
    <sql id="noIdCol">
        nid,fuid,tuid,pid,title,isdel,time
    </sql>

    <select id="findAll" resultType="com.tcm.community.model.NotifyPbravo">
        select * from tcm_notify_pbravo;
    </select>
</mapper>