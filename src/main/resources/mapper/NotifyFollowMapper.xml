<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcm.community.mapper.NotifyFollowMapper">
    <sql id="allCol">
        nfid,nid,fuid,tuid,isdel,time
    </sql>
    <sql id="noIdCol">
        nid,fuid,tuid,isdel,time
    </sql>

    <select id="findAll" resultType="com.tcm.community.model.NotifyFollow">
        select * from tcm_notify_follow;
    </select>
</mapper>