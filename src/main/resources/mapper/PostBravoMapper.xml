<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcm.community.mapper.PostBravoMapper">
    <sql id="allCol">
        pbid,pid,uid,isdel,optime
    </sql>
    <sql id="noIdCol">
        pid,uid,isdel,optime
    </sql>

    <select id="findAll" resultType="com.tcm.community.model.PostBravo">
        select * from tcm_post_bravo;
    </select>
</mapper>